# 错误收集

| 阶段 | 数量 | 位置 |
|---|---:|---|
| 开发 | 1 | 本文件 §1 |
| 测试/开发 | 11 | 本文件 §2–12；长文 [Agent-Docs/Mistake_Collect](./Agent-Docs/Mistake_Collect/Mistake_Collect_Index.md)（gitignore） |

### 1. 未批准缺失候选阻塞身份清单加载
**阶段**: 开发  
**现象**: `test_unapproved_or_missing_assets_are_not_promoted` 加载含缺失 `pending` 候选的身份清单时报错。  
**根因**: `validate_anchor` 对全部候选执行文件存在性校验，未区分已批准资产与待定候选。  
**解决**: 仅要求身份参考图及状态为 `approved` / `satisfied` 的候选文件存在。  
**教训**: 候选收集态与可进入生成链路的已批准态必须分开校验。  

### 2. 方案确认点进入下一步，intent 已 applied 阶段不前进
**阶段**: 测试  
**现象**: 看板方案确认提交后 `intents` 为 applied，`artifacts/` 未写，`checkpoint_brief_locked` 仍 in_progress，页面不进素材检查。  
**根因**: 活着的旧 runner 不写证据；旧 recovery 要求磁盘已有 artifacts 才能自愈，形成死锁。  
**解决**: `_recover_stuck_brief_locked` 在 applied 且无产物时补跑 `lock_gap_plan_from_intent`；open 按 code_stamp 换 runner（Plan 16，当时未 commit）。  
**教训**: 对 intent / checkpoint / artifacts 三份文件；改 runner 后必须换进程。长文：[Mistake_Collect/01](./Agent-Docs/Mistake_Collect/01-看板停点applied却不前进.md)。  

### 3. 素材检查开始出片无提示，并未调视频模型
**阶段**: 测试  
**现象**: 「开始出片」后主文案像制作中，无 generate 调用；`assets_gate` 未 completed，`maybe_start` 不开烧。  
**根因**: 同第 2 条旧 runner；gate 未闭环。  
**解决**: gate recovery；暂停主文案；paused 时不得把 overlay 盖回制作中。  
**教训**: 「制作中 + 分钟数」不等于已调模型。长文：[Mistake_Collect/02](./Agent-Docs/Mistake_Collect/02-开始出片无提示且伪装制作中.md)。  

### 4. open 发现 runner 仍存活就不重启
**阶段**: 开发  
**现象**: 更新 Python 后再 `python -m backlot open`，仍执行旧逻辑。  
**根因**: lock 仅 PID，alive 即复用。  
**解决**: `runner.lock` 写入 `{pid, code_stamp}`，stamp 不匹配先 stop 再起；另做「退出看板」。  
**教训**: 导出关 Python 挡不住「代码已更新、片未做完」。长文：[Mistake_Collect/03](./Agent-Docs/Mistake_Collect/03-旧runner不随open更新.md)。  

### 5. 重度 Pixverse 网页开烧伪装制作中
**阶段**: 开发  
**现象**: 已锁 TokenHub·Pixverse，开始出片后 `video_channel_missing`，页面仍写「制作中 16–28 分钟」。  
**根因**: `_PAID_PROVIDERS` 无 pixverse；制作中文案覆盖暂停 overlay。  
**解决**: 主文案「已暂停」并列出支持渠道；Plan 19 起混元/Pixverse 即使有 Key 也不可选开烧。  
**教训**: 创建页能选的渠道必须 ⊆ 开烧能跑的渠道，否则主文案暂停。长文同 §3。  

### 6. 未点同意/开始出片卡片仍可提交
**阶段**: 开发  
**现象**: 摘要「（尚未选择）」时底部提交仍可用。  
**根因**: `gapPlanReady` 在未选 continue 时返回 true。  
**解决**: 未修。操作上先点中间卡片再提交。  
**教训**: applied 不等于人选了 continue。长文：[Mistake_Collect/04](./Agent-Docs/Mistake_Collect/04-未选卡片仍可提交.md)。  

### 7. 仓根 pytest 误收集项目脚本并触发 Agnes 请求
**阶段**: 测试/开发
**现象**: 无付费 H0A 回归执行 `python -m pytest -q` 时，pytest 收集 `projects/jade-bracelet-promo/test_i2v.py`；模块导入立即 POST 到 Agnes，创建请求返回 503 后 collection 中止。
**根因**: `projects/` 未列入 `pytest.ini` 的 `norecursedirs`，而项目内手工实测脚本使用 `test_*.py` 且在模块顶层执行网络调用。
**解决**: `pytest.ini` 排除 `projects`；正式回归明确使用 `python -m pytest tests -q`；仓根全量前先做 collection-only。
**教训**: Git ignore 不等于 pytest ignore；真实 Provider 实测必须有显式入口和授权门，禁止模块导入即调用。长文：[Mistake_Collect/05](./Agent-Docs/Mistake_Collect/05-pytest误收集项目脚本触发付费API.md)。

### 8. 库页创建表单输入被 SSE 刷新清空
**阶段**: 开发  
**现象**: 库页「商品主题 / 时长 / 素材网址」刚打几个字就空白，像闪退。  
**根因**: `library.js` 的 `render()` 每次 SSE/`change` 或 4 秒轮询都 `renderOnboarding()`，整表换成新空 input。  
**解决**: 表单已挂载后只更新服务信息；换评审方式只替换步骤条，不重建输入框。  
**教训**: 轮询刷新只改卡片/计数，禁止销毁正在编辑的表单。

### 9. 项链占唯一 runner：创建被拒、结束导出无反应
**阶段**: 测试  
**现象**: 库页项链显示「进行中 · 待交付」；点开始创建 409；进项目点「结束并导出」无反应。  
**根因**: `commercial-20260818-20260818-02` 锁了 Pixverse，看板 generate 白名单无该渠道，实际已 paused；无 `renders/final.mp4` 故导出按钮禁用；唯一 runner 仍绑这个 id。库卡片 `live` 只看 5 分钟活跃，checkpoint 仍写「制作中」。  
**解决**: 操作上点「退出看板」停 runner 冻结，不结案。Plan 19：库页「放下再做别的」不停网页；无成片不显示制作中/待交付；混元/Pixverse 不可选开烧。  
**教训**: 中断 ≠ 导出；可选渠道必须 ⊆ 能烧渠道；占用 runner 必须在库页写明项目名。

### 10. 交付确认成片预览上下两个且播放闪退
**阶段**: 开发  
**现象**: 交付页播放框上下各一个成片；点播放几乎只能看前几秒，画面反复跳回开头。  
**根因**: `renderFinalVideo` 与 `renderCommercialPlayers` 各挂一个同 src 的 `<video>`；原生控件上又绑 click-to-play；SSE/轮询每次 `innerHTML` 清空重建播放器。  
**解决**: 交付/合成已有 hero 时不再 prepend 横幅；刷新前卸下并复用同一 video 节点；去掉 click-to-play。  
**教训**: 同一成片只留一个播放器；轮询不得销毁正在看的 video。

### 11. React 默认站漏迁制作档位面板
**阶段**: 开发  
**现象**: 新项目进方案确认后没有轻度/中度/重度和「确认开始出片」，runner 一直空闲。  
**根因**: 切默认 React 站时只迁了决策卡/导出/中断；旧站 `board-tier.js`（`.commercial-tier-panel` → `POST .../start-production`）未进 `BoardPage`。对照测用已锁定档位的 fixtures，挡不住。  
**解决**: 迁 `ProductionTierPanel`；未锁定时展开三档；锁定后折进「制作档位（已锁定：…）」；契约测检查选择器与 start-production。  
**教训**: 新旧站对照必须包含「未锁档位的新建项目」路径；vanilla 每个 `board-*.js` 都要在 React 有对应件或明确不做。长文：[Mistake_Collect/06](./Agent-Docs/Mistake_Collect/06-React看板漏迁制作档位面板.md)。

### 12. 缺图图生图停在素材检查且未调模型
**阶段**: 测试  
**现象**: 手链 `commercial-20260820` 已锁 Agnes 图生图，素材检查已暂停；点「开始出片」不生图、不生视频。  
**根因**: 03 要求 `assets_gate` 生图审图，runner 文件头写「不调付费 API」；方案页只锁计划；`seal_assets_gate` 见 `i2i_planned` 就拒绝。决策草稿跨阶段串台，一条 intent 里既有图生图又有开始出片。  
**解决**: Plan 32 第 1 刀：看板点「开始生成补图」后 runner 调已锁图生图；审图后再「开始出片」。浏览器仍不得调 API。  
**教训**: 「不静默硬烧」≠ runner 永远不调模型；用户看板点过的授权必须由本机 runner 执行。 |
