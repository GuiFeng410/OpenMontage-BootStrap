# archive/

只读历史归档（G6.1）。从仓根搬走的演示、报告与重复文档放这里，**不进**默认 CI，**不进**运行包 `core`。

| 路径 | 原位置 | 说明 |
|------|--------|------|
| `Commercial-Video/` | 仓根 | 商品片目录约定样例（多为占位） |
| `reports/` | 仓根 | 早期电商执行/可行性报告 |
| `错误收集.md` | 仓根 | 旧错误笔记；现行真源为 `Agent-Docs/Mistake_Collect/`（本机） |

需要时从本目录查阅；不要在生产代码里硬编码 `archive/` 路径。
