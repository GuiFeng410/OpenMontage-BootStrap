# 01 — 错误集合

> 归属：[README/错误处理](./README.md)。Playbook 动作见 [02-playbook说明.md](./02-playbook说明.md)。

## 索引

| 编号 | 阶段 | 标题 | 状态 |
|------|------|------|------|
| 1 | 测试 | BGM 素材无声（-91dB） | 已解决 |
| 2 | 开发 | FFmpeg 字幕滤镜 Windows 盘符冒号 | 已解决 |
| 3 | 开发 | PowerShell 参数转义破坏 FFmpeg 命令 | 已解决 |
| 4 | 开发 | amix + AAC 码率坍塌为 2kbps | 已解决 |
| 5 | 测试 | Agnes API 401：本地 Key 与有效 Key 不一致 | 已解决 |
| 6 | 开发 | Agnes 视频完成但取不到成片 URL | 已解决 |
| 7 | 测试 | Agnes 并行生视频 503 / 429 | 已解决 |
| 8 | 开发 | generation_manifest 残留 running 导致漏重试并误拼接 | 已解决 |

---

## 1. BGM 素材无声（-91dB ≈ "不存在"）

**阶段**: 测试  
**现象**: 从 Pixabay 下的 MP3（clouds.mp3, piano.mp3）编码到 AAC 后只有约 2kbps，几乎静音。  
**根因**: `ffprobe loudnorm` 显示 `input_i: -inf`，`volumedetect` 为 `mean_volume: -91.0 dB`。第三方库返回了无效占位音频。  
**解决**: 用零 Key 合成 ambient BGM，128k AAC 编码。  
**教训**: Stock 音乐下载后必须先 `volumedetect` / loudnorm 验响度再混音。对应 Playbook **E01**。

## 2. FFmpeg 字幕滤镜在 Windows 路径上崩溃

**阶段**: 开发  
**现象**: `subtitles=D:\workfile\...\sub.srt` 报 `Unable to parse "original_size" option value`。  
**根因**: FFmpeg `subtitles` 滤镜把 `D:` 的冒号当成 `option:value` 分隔符。  
**解决**: 将 SRT 复制到无盘符相对路径（如 `assets/subs/_work/`）再烧录。  
**教训**: Windows 盘符路径勿直接塞进 FFmpeg filter。对应 Playbook **E02**。

## 3. PowerShell 的参数传递问题

**阶段**: 开发  
**现象**: 同一 FFmpeg 命令在 PowerShell 与 cmd/Python 行为不同（`&&` 截断、逗号被吃、引号嵌套失败）。  
**根因**: PowerShell 把 `&`、`,`、引号按自身语法解析，不是按 cmd/POSIX shell。  
**解决**: 全程 `subprocess.run([...], shell=False)` list 传参。  
**教训**: 媒体命令不要拼进 PowerShell 字符串。对应 Playbook **E03**。

## 4. FFmpeg `amix` 混音输出码率灾难

**阶段**: 开发  
**现象**: 长 WAV 混音后 AAC 仅 18–30KB，码率 2–4kbps。  
**根因**: 输入近静音时 AAC 自动降到最低码率。  
**解决**: 先出 WAV PCM，再单独 `-b:a 128k` 编码并验码率。  
**教训**: 混音与编码拆成可验证两步。对应 Playbook **E04**。

---

## 5. Agnes API 401：本地 Key 与有效 Key 不一致

**阶段**: 测试  
**现象**: `.env` 已填 `AGNES_API_KEY`，本机工具显示 `available`，但调用 `apihub.agnes-ai.com` 的 chat/image/video 均返回 **401 无效的令牌**；用户在 cmd 用 curl 同一账号 Key 却成功。  
**根因**:
1. `.env` 中的 Key 与 curl 验证成功的 Key **差 1 个字符**（粘贴/误改导致），服务端按「错误 Key」拒绝；
2. 过程中曾把 Key「纠正」成错误拼写，加剧不一致；
3. 本地只检查「变量非空」，**不能**等同于「云端鉴权通过」。  
**解决**:
1. 以用户 curl 成功的完整 Key 为准写回 `.env`（单行、无引号、无空格）；
2. 生成前用轻量 `POST /v1/chat/completions` 做鉴权探针（期望 HTTP 200）；
3. 勿根据 OCR/猜测改 Key。  
**教训**:
- Agnes **401** 优先查 Key 是否完整、是否与控制台/已验证 curl 一致，而不是先改业务代码；
- 接入新供应商时增加「鉴权探针」再跑付费/耗时视频任务；
- Agent 不得擅自改写用户密钥字符。

## 6. Agnes 视频任务完成但取不到成片 URL

**阶段**: 开发  
**现象**: Key 有效后，`agnes_video` 创建任务成功并轮询结束，却报 `Agnes video output missing metadata.url`，本地无 mp4。  
**根因**:
1. 文档示例把成片放在 `metadata.url`；
2. **实况 API** 完成态把下载地址放在**顶层** `url`，且常无 `metadata` 字段；
3. 状态机除 `queued` / `in_progress` / `completed` 外，还会出现 **`pending`**，旧逻辑只认文档状态集合。  
**解决**（已改 `tools/video/agnes_video.py`）:
1. 取 URL：`result.url` → `metadata.url` → `video_url` 依次回退；
2. 轮询：将 `pending` 视为进行中；失败态兼容 `failed` / `error` / `cancelled`；
3. 单测改为覆盖顶层 `url` 实况形状。  
**教训**:
- 供应商文档与实况响应可能不一致，**以一次真实 create→poll 的 JSON 字段为准**写解析；
- 异步视频工具应对「文档字段 + 实况字段」做兼容，并在缺失 URL 时打印可用 key 列表便于排查；
- 样片宜先 3–5 秒验证链路，再拉长时长。

## 7. Agnes 并行生视频 503 / 429

**阶段**: 测试  
**现象**: `llm-explainer-30s` 按规范并发 3 路生成时，scene01 成功；scene02 连续报 **503 Service Unavailable**（`POST /v1/videos`）；scene03 创建后轮询报 **429 Too Many Requests**（`/agnesapi?video_id=...`）。整轮失败片段无 mp4。  
**根因**:
1. Agnes 云端在短时间内多路 create/poll 时易过载或限流；
2. 503 与 429 均为可重试错误，但**高并发会放大失败率**，退避重试仍可能耗尽；
3. 与成片时长无关——慢的是排队/限流，不是 30s 文件本身。  
**解决**:
1. 降并发为 **1**，只补跑 `failed`/`pending` 段（已有 scene01 跳过）；
2. 保留 503/502/429/timeout 退避重试；限流明显时优先降并发而非盲目加长重试；
3. 编排规范默认并发 2–3，遇 503/429 尖峰自动降到 1。  
**教训**:
- 并行编排的瓶颈在 **provider 限流**，不是本地 FFmpeg；
- 首次新项目可先并发 2 试水；失败后「跳过已有 + 串行补跑」；
- 进度以 `generation_manifest.json` 为准，勿因一轮失败重做全部片段；
- **官方视频 RPM**：default 实际 **1**、enterprise **2**、TokenPlan **5**（见 [Token Plan FAQ](https://agnes-ai.com/zh-Hans/docs/tokenplan)）。默认并发 3 对 default 档过激进——详细复盘与改法见 [03-并行耗时与Agnes限流分析.md](./03-并行耗时与Agnes限流分析.md)。

## 8. generation_manifest 残留 running 导致漏重试并误拼接

**阶段**: 开发  
**现象**: scene02/03 失败后再次 `--mode generate --assemble-after`，几乎立刻结束并进入拼接，报 `FileNotFoundError: missing clip for scene02`；manifest 里两段状态卡在 **`running`**，且仍带着上一轮的 error 文案。  
**根因**:
1. `pending_scene_ids()` 旧逻辑只认 `pending` / `failed`，**不认** 中断或异常退出留下的 `running`；
2. 二次开跑时先把待办标成 `running` 后若任务池空跑/提前返回，状态既不是 `failed` 也不在待办里；
3. CLI 只检查 `status == failed` 才阻断，**漏检 `running`**，于是带着残缺片段去 `assemble_ffmpeg`。  
**解决**（已改 `lib/parallel_generate.py` + `scripts/run_parallel_video.py`）:
1. `pending_scene_ids` 纳入 `running`（视为孤儿任务，需重试）；
2. 进入 `running` 时清空陈旧 `error`；
3. generate 结束条件改为：非 `completed`/`skipped` 一律视为未完成，禁止拼接。  
**教训**:
- 幂等重跑必须覆盖 **pending / failed / orphaned running** 三种态；
- 「可以拼接」的判定是 **全部片段落盘且状态完成**，不是「没有 failed」；
- 长任务中断后先 ` --mode status` 看 manifest，再决定补跑还是降并发。

---

## 附录：早期叙述稿（与上表 1–4 对应，保留备查）

<details>
<summary>展开旧版长文叙述</summary>

### BGM / 字幕 / PowerShell / amix（原文摘要）

1. Pixabay 无效静音素材 → 合成 BGM  
2. Windows `D:` 盘符破坏 subtitles 滤镜 → 相对路径  
3. PowerShell 转义 → Python subprocess list  
4. amix+AAC 码率坍塌 → 两步编码  

</details>
