OpenMontage BootStrap — 已装机器补装（最小改动）

前提：本机已有仓库 + 门面 MCP + setup/produce，只补新内容。
全新安装请看：流程简版-OpenClaw安装到出片.txt

不动：门面 MCP、setup、produce、skills 目录配置（extraDirs 一般已指向仓内 skills）。

------------------------------------------------------------
1. 拉代码（唯一必做的本机动作）
------------------------------------------------------------

cd <REPO>
git pull bootstrap main
# 或：git pull gitee main

拉完后确认有：
  openmontage/mcp/providers_{tts,image,video,stock}/
  openmontage/skills/openmontage-bootstrap-providers/
  openmontage/skills/openmontage-bootstrap-produce/   （含三档流程）
  openmontage/skills/openmontage-providers-{tts,image,video,stock}/
  README/templates/providers-*.mcp.json
  README/06-出片三档说明.md

（可选）.\.venv\Scripts\Activate.ps1 && pip install -r requirements.txt

------------------------------------------------------------
2. OpenClaw：只「新增」，不改旧项
------------------------------------------------------------

MCP（模板在 README/templates/；command/cwd/env 复制门面那份即可；Key 可空）：

  + openmontage-providers-tts     ← providers-tts.mcp.json
  + openmontage-providers-image   ← providers-image.mcp.json
  + openmontage-providers-video   ← providers-video.mcp.json
  + openmontage-providers-stock   ← providers-stock.mcp.json（可选，要免费素材再加）

Skill（extraDirs 已有则只开开关）：

  + openmontage-bootstrap-providers     （建议开）
  + openmontage-providers-tts/image/video  （要用付费再开）
  + openmontage-providers-stock            （配了 stock MCP 再开）

重启/重载 MCP 后确认新 server 能起来；旧门面仍正常即可。

------------------------------------------------------------
3. Key（用到再填，不填也能继续零 Key 出片）
------------------------------------------------------------

付费 → 对应 MCP 的 env，见 04-收费Providers接入.md
Stock → PEXELS_API_KEY / PIXABAY_API_KEY，见 05-免费Stock素材接入.md
填完重启该 MCP。

------------------------------------------------------------
4. 检查完就算过关
------------------------------------------------------------

[ ] git pull 成功
[ ] 门面仍能用；setup 未动；produce Skill 已是含三档的新版
[ ] 新增 3 个付费 MCP 能启动；Skill03 已启用
[ ] （可选）stock MCP + Skill；Key 有需要再填

出片：主题确认后选轻度/中度/重度，见 README/06-出片三档说明.md。

（可选）外置 installer 若以前拷过：用仓内最新夹覆盖一次（含更细 MCP 验收步骤），不影响现有出片。
