[pytest]
# BootStrap / CI: skip script-style QA modules (module-level side effects, not pytest tests).
norecursedirs = qa projects .git .venv venv node_modules remotion-composer __pycache__
pythonpath = src
python_files = test_*.py
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
