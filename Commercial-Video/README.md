# Commercial-Video

商品宣传视频的独立工作区。

每个商品视频使用一个独立项目目录：

```text
Commercial-Video/
└── <product-project>/
```

长于或等于 30 秒的商品视频，按照：

```text
总览表 -> 分批审查卡 -> 修改清单确认 -> 分批锁定 -> 整片合成
```

现有 pipeline 运行目录仍由项目 Manifest 记录。不要为了整理目录而移动已经生成的素材。
