# P1 验收清单

> 对应终态：[../../chance_file/03-P0-P1-P2阶段最终实现形态.md](../../chance_file/03-P0-P1-P2阶段最终实现形态.md) §17

## A. 自动化（本机）

```powershell
cd <OPENMONTAGE_REPO_ROOT>
.\.venv\Scripts\Activate.ps1
$env:PYTHONUTF8 = "1"
$env:OPENMONTAGE_PROJECTS_DIR = "$PWD\projects"
python -m pytest tests/mcp_p0 tests/mcp_p1 -q
```

- [ ] `tests/mcp_p0` 通过
- [ ] `tests/mcp_p1` 通过（写盘门控、TTS confirm、job store、沙箱）
- [ ] `run_doctor()`：`can_produce_video_now` 在依赖齐全时为 `true`
- [ ] 无 `OPENMONTAGE_P1_ALLOW_WRITES` 时 `init_project` / `write_artifact` 失败
- [ ] `tts_generate(..., confirm_sample_ok=False)` 拒绝

## B. OpenClaw 配置

- [ ] Default Agent 看不到 / 不能成功调用写工具与 media 写路径
- [ ] Production Agent 同时挂载 doctor + media
- [ ] Skills：explainer + production-contract 已加载

## C. 端到端对话（推荐）

在无付费 Key 环境完成一条 30～60 秒解说：

- [ ] proposal / script / scene_plan / assets 真实停轮
- [ ] 未批准不得进入下一阶段（`approve_checkpoint` 需用户原文）
- [ ] Piper 样本通过后才批量 TTS
- [ ] canonical artifacts schema-valid（`validate_artifact`）
- [ ] 中断后 `get_next_stage` 可恢复
- [ ] `final.mp4` 经 `probe_media` / ffprobe 通过
- [ ] 时长在目标 ±5%（人工核对）
- [ ] 工具失败时给出 blocker，不静默换 Remotion↔HyperFrames

## D. 回归边界

- [ ] 默认 Agent 仍符合 P0：不能出片、不能写宿主盘
- [ ] 无 `make_video` 单工具出片路径
