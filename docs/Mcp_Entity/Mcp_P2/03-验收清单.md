# P2 高级 TTS 验收清单

## A. 自动化

```powershell
cd <OPENMONTAGE_REPO_ROOT>
.\.venv\Scripts\Activate.ps1
$env:PYTHONUTF8 = "1"
$env:OPENMONTAGE_PROJECTS_DIR = "$PWD\projects"
python -m pytest tests/mcp_p2 -q
```

- [ ] `tests/mcp_p2` 通过  
- [ ] 无 `confirm_*` 时 sample/generate 拒绝  
- [ ] `OPENMONTAGE_ALLOWED_PROVIDERS` 过滤生效  

## B. OpenClaw

- [ ] Production 可见 `openmontage-providers-tts__*`  
- [ ] Default 可不挂此 MCP  
- [ ] Skill `openmontage-providers-tts` 已启用  

## C. 有 Key 人工（可选）

- [ ] `tts_dry_run` 返回合理估价  
- [ ] sample 写入沙箱并可播放  
- [ ] 故意失败时不自动换到另一家云 TTS  

## D. 回归

- [ ] 不装本 MCP 时 P1 Piper 路径仍可用  
