# Bootstrap 验收清单

- [ ] `python -m openmontage.mcp.bootstrap` 可启动（stdio）  
- [ ] `list_bootstrap_tools` 含 bootstrap + produce_minimal，且标明 not_in_v1  
- [ ] 高危工具默认 dry_run；无 `confirm_execute` 时执行抛 ConfigError  
- [ ] `plan_install` 无副作用  
- [ ] `ensure_ffmpeg` 失败路径返回 `manual_commands`  
- [ ] Skill01 / Skill02 目录存在且可被 OpenClaw 加载  
- [ ] `pytest tests/mcp_bootstrap -q` 通过  
- [ ] 手动 clone + 仅门面 MCP 可按文档配置（实机）  
- [ ] （实机）`verify_ready` 后能走出片到 `renders/final.mp4`  
